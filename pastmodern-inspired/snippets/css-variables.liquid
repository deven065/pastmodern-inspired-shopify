{% style %}
  {%- comment -%}Body font — load all four variants{%- endcomment -%}
  {{ settings.type_body_font | font_face: font_display: 'swap' }}
  {{ settings.type_body_font | font_modify: 'weight', 'bold' | font_face: font_display: 'swap' }}
  {{ settings.type_body_font | font_modify: 'weight', 'bold' | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}
  {{ settings.type_body_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}

  {%- comment -%}Heading font — load regular, bold, italic{%- endcomment -%}
  {{ settings.type_heading_font | font_face: font_display: 'swap' }}
  {{ settings.type_heading_font | font_modify: 'weight', 'bold' | font_face: font_display: 'swap' }}
  {{ settings.type_heading_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}

  :root {
    /* ── Typography ─────────────────────────────────────────── */
    --font-body:         {{ settings.type_body_font.family }}, {{ settings.type_body_font.fallback_families }};
    --font-body-weight:  {{ settings.type_body_font.weight }};
    --font-body-style:   {{ settings.type_body_font.style }};

    --font-heading:        {{ settings.type_heading_font.family }}, {{ settings.type_heading_font.fallback_families }};
    --font-heading-weight: {{ settings.type_heading_font.weight }};
    --font-heading-style:  {{ settings.type_heading_font.style }};

    /* ── Colors — merchant-controllable ────────────────────── */
    --color-bg:     {{ settings.color_background }};
    --color-text:   {{ settings.color_text }};
    --color-accent: {{ settings.color_accent }};

    /* ── Colors — Sulu Luxe fixed palette ───────────────────── */
    /* Neutrals */
    --color-bg-warm:        #EFE7DA;   /* Warm Cream — subtle section variation   */
    --color-bg-stone:       #DCCFBE;   /* Stone Beige — stronger section contrast */
    --color-bg-subtle:      #EFE7DA;   /* Alias → Warm Cream (used for image bgs) */
    --color-border:         #E7DFD2;   /* Mist Border — borders, dividers, inputs */
    --color-text-muted:     #8A847C;   /* Warm mid-tone — captions, secondary UI  */
    --color-text-secondary: #4A4944;   /* Soft Graphite — secondary body text     */
    --color-overlay:        {{ settings.color_text | color_modify: 'alpha', 0.48 }};

    /* Emerald family */
    --color-accent-hover:   #2B5F41;   /* Heritage Green — hover on primary accent */
    --color-accent-soft:    #366F4C;   /* Soft Emerald — lighter accent context    */
    --color-sage:           #7D947F;   /* Muted Sage — subtle green tint           */
    --color-accent-subtle:  color-mix(in srgb, {{ settings.color_accent }} 8%, transparent);
    --color-accent-dim:     color-mix(in srgb, {{ settings.color_accent }} 18%, transparent);

    /* Gold family — sparingly as micro-luxury accents */
    --color-gold:           #D6C08A;   /* Antique Gold — stars, micro accents  */
    --color-gold-soft:      #C8B27A;   /* Soft Gold — hover/alternate gold     */

    /* Sand */
    --color-sand:           #B7AA98;   /* Sand Taupe — UI separation, muted elements */

    /* ── Layout ─────────────────────────────────────────────────── */
    --page-width:    {{ settings.page_width }};
    --page-margin:   {{ settings.page_margin }}px;
    --header-height: 5.5rem;

    /* ── Shape ──────────────────────────────────────────────── */
    --radius-input: {{ settings.input_radius }}px;
    /* ── Merchant scales ─────────────────────────────────── */
    --heading-scale:  {{ settings.heading_scale | default: 1.0 }};
    --base-font-size: {{ settings.body_font_size | default: '0.9375rem' }};
    --logo-max-width: {{ settings.logo_max_width | default: 140 }}px;
    {%- if settings.button_style == 'soft' -%}
      --btn-radius: 2px;
    {%- elsif settings.button_style == 'rounded' -%}
      --btn-radius: 6px;
    {%- else -%}
      --btn-radius: 0px;
    {%- endif -%}
    --header-position: {% if settings.header_sticky == false %}relative{% else %}sticky{% endif %};

    /* ── Section vertical rhythm ─────────────────────────── */
    {%- if settings.section_spacing == 'compact' -%}
      --section-spacing: clamp(1.75rem, 3.5vw, 3rem);
    {%- elsif settings.section_spacing == 'comfortable' -%}
      --section-spacing: clamp(2.75rem, 5.5vw, 5rem);
    {%- else -%}
      --section-spacing: clamp(4rem, 8vw, 8rem);
    {%- endif -%}  }
{% endstyle %}
